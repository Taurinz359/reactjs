class LoginStore {
  username = 'kminchelle';
  password = '0lelplR';

  setUsername(name) {
    this.username = name;
  }

  setPassword(password) {
    this.password = password;
  }
}

export default LoginStore;
