class newStore {
  title = '';
  body = '';
  urlToImage = '';

  setTitle(title) {
    this.title = title;
  }

  setBody(text) {
    this.body = text;
  }

  setUrlToImage(url) {
    this.urlToImage = url;
  }
}

export default newStore;
